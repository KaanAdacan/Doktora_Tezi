# Stage 03 matched 10-ps policy

Production only. MAPK native save interval = 10 ps and stride = 1. ADK native save interval = 2 ps and stride = 5. Both yield the exact common time grid 0.01, 0.02, ..., 100.00 ns (10,000 frames). Raw trajectories remain read-only. No interpolation, smoothing, filtering, imputation or outlier removal is performed. RMSD, backbone RMSD, Rg and RMSF are recomputed on this matched frame set.
